// Test NF3: assignments, tranfers, require

contract Test2 {
    int x; 
  
    function f(address a, int y) {
        x = x + y;
        a.transfer(x:T);
        require(x>5 && balance(T)>7);
        x = x + balance(T);
    }
}