// Test NF2: assignments, tranfers, local variables

contract Test2 {
    int x; 
    int y;
   
    function f(address a, int z) {
        y = x + y + balance(T);
        x = x+y+2;
        w = x;
        x = w;
        x = x + x + 1;
        a.transfer(x:T);
        a.transfer(y:T);
        y = balance(T) + z;
        w = w+1;
        // x,y,T_fin = x_2,y_2,T_2;
    }
}