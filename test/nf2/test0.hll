contract Test0 {
    int x; 
    int y;

    constructor() { }
    
    // NF1
    function f(address a, int z) {
        y = x + y + balance(T);
        x = x+y+2;
        x = x+1;
        a.transfer(x:T);
        a.transfer(y:T);
        y = balance(T) + z;
    }
}