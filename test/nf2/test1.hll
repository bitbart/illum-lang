contract Test1 {
    int x; 
  
    // IS_NF2
    function f(address a, int y) {
        if (x>20) {
            x = x + y;
        }
        else {
            x = y;
        }
        a.transfer(x:T);
        require(x>0 && balance(T)>7);
        x = x + 1;
    }
}