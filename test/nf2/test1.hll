contract Test1 {
    int x; 
    int y;

    constructor() { }
    
    // NF1
    function f(address a) {
        // require(n<5); 
        if (x == 0)
            x = 1;
        else {
            x = 2;
            a.transfer(1:T);
            a.transfer(1:T2);
        }
    }
}