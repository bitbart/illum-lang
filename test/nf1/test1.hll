contract Test1 {
    int n; 
    int m;
  
    function f(address a) { 
        a.transfer(1:T);
        if (balance(T) == 0)
            n = balance(T);
        else
            n = balance(T2); 
    }
}